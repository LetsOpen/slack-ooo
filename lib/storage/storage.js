/**
 * @module Storage
 */

class Storage {

  /**
   * @constructor
   * @param {Object} config Any configuration for the Object
   */
  constructor (config) {
    this.config = config;
  }
}

module.exports = {
  interface: Storage
};
